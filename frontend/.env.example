# Strata 前端环境变量
# 使用方式：cp .env.example .env
# 开发环境 Vite 自动注入，生产环境通过 nginx 在编译前设置

# API 基础 URL（为空时使用 Vite 代理 /api/* → localhost:8000）
VITE_API_BASE_URL=

# WebSocket 基础 URL（为空时根据当前页面协议和主机自动推导）
# 开发：ws://localhost:8000
# 生产：wss://your-domain.com
VITE_WS_BASE_URL=
